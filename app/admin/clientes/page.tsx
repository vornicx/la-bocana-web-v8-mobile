import { reservations } from '@/lib/admin/mock-data';
export default function CustomersPage(){return <div className="admin-page"><div className="admin-page-head"><div><span className="admin-kicker">Mini CRM</span><h1>Clientes</h1><p>Historial y preferencias asociadas a las reservas.</p></div></div><div className="customer-grid">{reservations.slice(0,6).map(r=><article className="customer-card" key={r.id}><div className="customer-initials">{r.customer.split(' ').map(x=>x[0]).slice(0,2).join('')}</div><div><h3>{r.customer}</h3><p>{r.phone}</p><div className="customer-meta"><span>{r.visits} visitas</span><span>{r.allergies?'⚑ Alergia registrada':'Sin alertas'}</span></div>{r.notes&&<small>{r.notes}</small>}</div></article>)}</div></div>}
